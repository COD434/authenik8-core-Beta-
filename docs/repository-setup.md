# Repository Setup

To connect an existing local repository to the upstream GitLab project:

```sh
git remote add origin https://gitlab.com/COD434/authenik8-core.git
git branch -M main
git push -uf origin main
```

Verify the target remote and branch before pushing.

[Back to the documentation index](../README.md#documentation)
